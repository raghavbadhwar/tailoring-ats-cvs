# Troubleshooting

`bootstrap_required` and `upgrade_required` require user permission before installation. `manual_install_required` means neither supported installer is available; use the pinned manual virtual-environment command in the installation reference. Never bypass a failed strict doctor check.
