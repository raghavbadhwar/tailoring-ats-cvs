# Approval policy

The job description and company context are never candidate evidence. Only explicitly selected supported change IDs may be approved, and a selection needs a variant ID where variants exist. There is no blanket approval. Do not overwrite the source CV or use `--force` unless the user explicitly requests replacement of an existing generated output. A stale or tampered proposal needs a new `prepare`; unsupported gaps are reported, not inserted.
